// All types are now imported from '../types'
import { ResponsibleType, Responsible } from '../types';

// Re-export all types for backward compatibility
export type { ResponsibleType, Responsible };
